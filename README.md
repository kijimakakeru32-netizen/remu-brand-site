# ReMu Brand Site

ReMu logo selection and product mockup gallery.

## Local preview

Open `index.html` in a browser, or run a local static server.

## Netlify

Publish directory: repository root (`.`).
